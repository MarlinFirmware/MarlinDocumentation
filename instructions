Install Ruby 3.3.4 on Ubuntu 22.04.4 LTS

I Installed https://github.com/rbenv/rbenv as the os version is not recent

I mostly followed https://shape.host/resources/step-by-step-guide-installing-ruby-on-rails-with-rbenv-on-ubuntu-22-04

To start the installation process, we first need to update the package list:

  sudo apt update
  Next, we will install the dependencies required to install Ruby:
  
  sudo apt install git curl libssl-dev libreadline-dev zlib1g-dev autoconf bison build-essential libyaml-dev libreadline-dev libncurses5-dev libffi-dev libgdbm-dev
  Once the dependencies are installed, we can proceed to install rbenv itself. Use the following command to fetch the install script from Github and run it:
  
  curl -fsSL https://github.com/rbenv/rbenv-installer/raw/HEAD/bin/rbenv-installer | bash
  After the installation is complete, we need to add rbenv to our PATH environment variable. Modify the ~/.bashrc file to include the rbenv command line utility:
  
  echo 'export PATH="$HOME/.rbenv/bin:$PATH"' >> ~/.bashrc
  Additionally, add the following line to the ~/.bashrc file to ensure rbenv loads automatically:
  
  echo 'eval "$(rbenv init -)"' >> ~/.bashrc
  To apply the changes made to the ~/.bashrc file, run the following command:
  
  source ~/.bashrc

This also says how to install ruby 3.3.4

To install Ruby version 3.3.4, use:

  rbenv install 3.3.4
  The installation process may take some time to complete. Once it’s done, set the installed version as the global default:
  
  rbenv global 3.3.4
  To verify that Ruby is installed correctly, check its version:
  
  ruby -v

Then install bundler gems 

echo '# Install Ruby Gems to ~/gems' >> ~/.bashrc
echo 'export GEM_HOME="$HOME/gems"' >> ~/.bashrc
echo 'export PATH="$HOME/gems/bin:$PATH"' >> ~/.bashrc
source ~/.bashrc

gem install bundler

Get MarlinDocumentation

git clone https://github.com/MarlinFirmware/MarlinDocumentation.git

Update Gemfile with new gems versions

diff --git a/Gemfile b/Gemfile
index 9e4a02a..dabcd1d 100644
--- a/Gemfile
+++ b/Gemfile
@@ -1,17 +1,18 @@
 source 'https://rubygems.org'
 
-gem 'github-pages', '>= 207'
-gem 'html-proofer', '>= 3.18.5'
-gem 'jekyll', '= 3.9.0'
-gem 'jekyll-avatar', '>= 0.7.0'
+gem 'github-pages', '= 232'
+#gem 'html-proofer', '>= 3.18.5'
+gem 'jekyll', '= 3.10.0'
+gem 'jekyll-avatar', '>= 0.8.0'
 gem 'bootstrap', '>= 4.1.3'
 gem 'jekyll-bootstrap-sass', '>= 0.1.0'
-gem 'jekyll-mentions', '>= 1.5.1'
+gem 'jekyll-mentions', '= 1.6.0'
 gem 'jekyll-press', '>= 0.2.1'
-gem 'jekyll-seo-tag', '>= 2.6.1'
+gem 'jekyll-seo-tag', '= 2.8.0'
 gem 'jekyll-tidy', '>= 0.2.2'
 gem 'kramdown', '>= 2.3.0'
 gem 'naturally', '>= 2.2.1'
 gem 'wdm', '>= 0.1.0' if Gem.win_platform?
 gem 'tzinfo-data', '>= 1.2020.1'
 gem "webrick", "~> 1.7"

update _plugins/sitemap_generator.rb "File.exists?" becomes "File.exist?"

diff --git a/_plugins/sitemap_generator.rb b/_plugins/sitemap_generator.rb
index 2ab401f..43ec63f 100644
--- a/_plugins/sitemap_generator.rb
+++ b/_plugins/sitemap_generator.rb
@@ -126,7 +126,7 @@ module Jekyll
     def fill_pages(site, urlset)
       site.pages.each do |page|
         if !excluded?(site, page.path_to_source)
-          if File.exists?(page.path)
+          if File.exist?(page.path)
             url = fill_url(site, page)
             urlset.add_element(url)
           end


change into MarlinDocumentation directory

bundle config set path 'vendor/bundle'
bundle install
bundle exec jekyll serve --watch --incremental

bundle exec jekyll -v